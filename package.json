{
  "name": "blackrock-agent-core",
  "private": true,
  "version": "0.1.0",
  "description": "Reusable AI workspace shell + agent runtime, embedded into BlackRock AI client apps.",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter='*' build",
    "typecheck": "bun run --filter='*' typecheck"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.4.5"
  }
}
