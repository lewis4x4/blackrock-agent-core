@blackrock-ai:registry=https://npm.pkg.github.com
# GitHub Packages requires a token with read:packages (install) or write:packages (publish). Set GITHUB_TOKEN in your shell, or replace this with your token.
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
