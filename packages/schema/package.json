{
  "name": "@blackrock/agent-schema",
  "version": "0.1.0",
  "description": "Agent Core database migrations — applied into each client's own Supabase project.",
  "files": ["migrations"],
  "scripts": {
    "build": "echo 'schema: no build step'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@blackrock/agent-core": "workspace:*",
    "@blackrock/agent-runtime": "workspace:*",
    "@blackrock/agent-tools": "workspace:*",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
